### idx-linux-dependencies
dependencies for imfluxxy/idx-linux (https://github.com/imfluxxy/idx-linux)

contains bootloader, uefi files and the current distro (arch linux) iso for idx environment, allow others to download or do whatever they want
